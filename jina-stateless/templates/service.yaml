apiVersion: v1
kind: Service
metadata:
  name: {{ include "jina-stateless.fullname" . }}
  labels:
    {{- include "jina-stateless.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jina-stateless.selectorLabels" . | nindent 4 }}
