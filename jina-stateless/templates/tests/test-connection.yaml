apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jina-stateless.fullname" . }}-test-connection"
  labels:
    {{- include "jina-stateless.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jina-stateless.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
